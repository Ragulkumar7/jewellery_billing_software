packages:
  - "apps/*"
  - "packages/*"

# pnpm >= 10 refuses to run dependency install scripts unless explicitly
# allowed here; without esbuild's script a fresh clone cannot run vite/tsup.
onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
