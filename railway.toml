# Railway service config. Build recipe lives in nixpacks.toml (full phase
# control — Railway's config schema has no install-command hook).

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm --filter admin-api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
